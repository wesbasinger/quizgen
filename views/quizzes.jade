extends ./layout.jade

block title
  title Quizzes

block content
  h1 All quizzes
  ul
    each val in data
      li
        a(href='/quiz/'+val.slug+'?email='+email) val.slug
  h2 Logged in as
  p= email
